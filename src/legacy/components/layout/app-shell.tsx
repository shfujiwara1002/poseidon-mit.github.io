/**
 * AppShell — Application-level wrapper (Architecture B layout layer).
 *
 * Re-exports the existing AppShell with a streamlined API
 * aligned to the v0 adaptation workflow.
 */
export { AppShell } from '@/components/AppShell'
